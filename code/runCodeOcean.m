% This is a special script only on the code ocean branch that runs all
% demos, tests and generates unit cell images.
runTests();

runDemos();

addpath('../data');
plotUnitCell();
addpath('../data');